# Mantém src/public/index.html/configs no contexto — o Dockerfile multi-stage
# compila a partir do código-fonte. Só exclui o que não é necessário pro build.
node_modules
.git
.github
dist
tests
*.md
bun.lock
bun.lockb
